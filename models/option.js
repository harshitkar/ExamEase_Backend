class Option {
    constructor(id, questionId, optionText, imageUrl) {
      this.id = id;
      this.questionId = questionId;
      this.optionText = optionText;
      this.imageUrl = imageUrl;
    }
  }
  
  module.exports = Option;
  