class Question {
    constructor(id, testId, questionText, imageUrl) {
      this.id = id;
      this.testId = testId;
      this.questionText = questionText;
      this.imageUrl = imageUrl;
    }
  }
  
  module.exports = Question;
  